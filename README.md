# Sentinel

Sentinel is a transparent MitM proxy for OpenAI-compatible LLM APIs. It sits between clients and upstream providers, adding semantic caching, streaming PII redaction, and deep observability.
